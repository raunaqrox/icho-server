const assert = require('assert')

describe('Flow', () => {
  it('should pass this test', () => {
    // fill it later
    assert.equal(1, [1,2,3].indexOf(2))
  })
  it('should fail this test', () => {
    // fill it later
    throw new Error("I must fail you")
  })
})